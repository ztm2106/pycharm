#import exercise11
import exercise11

# varialbe p to prompt user and store data of the number of max asterisks
p = float(input("Enter a number of asterisks to build triangle: "))

#variable ast_ hold the transformed data from the function draw_triangle()
ast_ = exercise11.draw_triangle(p)

#print the triangle of asterisks from the info stores in the ast_ variable
print(ast_)